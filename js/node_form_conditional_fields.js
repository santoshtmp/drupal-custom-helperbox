/**
 * @file
 * Node form conditional fields behaviors.
 *
 * Provides conditional field visibility on node forms.
 * Currently empty - add behaviors here as needed.
 */

(function ($, Drupal, drupalSettings, once) {
  'use strict';

  /**
   * Attach node form conditional fields behaviors.
   *
   * @type {Drupal~behavior}
   *
   * @prop {Drupal~behaviorAttach} attach
   *   Attaches conditional field functionality on node forms.
   */
  Drupal.behaviors.nodeFormConditionalFields = {
    attach: function (context) {
      // Add conditional field behaviors here.
    }
  };

})(jQuery, Drupal, drupalSettings, once);
