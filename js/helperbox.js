/**
 * @file
 * Helperbox theme behaviors.
 *
 * Reserved for front-end/theme-specific JavaScript functionality.
 * Currently empty - add behaviors here as needed.
 */

(function ($, Drupal, drupalSettings, once) {
  'use strict';

  /**
   * Attach helperbox theme behaviors.
   *
   * @type {Drupal~behavior}
   *
   * @prop {Drupal~behaviorAttach} attach
   *   Attaches helperbox theme functionality.
   */
  Drupal.behaviors.helperboxTheme = {
    attach: function (context) {
      // Add theme-specific behaviors here.
    }
  };

})(jQuery, Drupal, drupalSettings, once);
